name: cell_pulse
description: "LiPo Health Monitor"
publish_to: 'none'
version: 0.1.0+dev

environment:
  sdk: ^3.13.1
  flutter: 3.47.1

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.3.0
  package_info_plus: ^10.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
  assets:
    - master_logo.svg
